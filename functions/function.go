package functions

import "fmt"

/*
	⚙️ Function parameters, return values, together with types, is called function signature.

	📌 Normal Function cannot be declared inside another function. If we want to achieve this, we can do this by anonymous function.
*/
func NormalFunction() {
	fmt.Println("Hey 👋, I'm normal function.")
}
